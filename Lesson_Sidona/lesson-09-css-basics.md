# Урок 9: Основы CSS стилизации

## Цели урока
- Изучить каскадность CSS
- Специфичность селекторов
- Организация CSS кода
- Лучшие практики

## 9.1 Каскадность CSS

CSS означает **Cascading Style Sheets** (Каскадные таблицы стилей).

### Порядок применения стилей:
1. Браузерные стили (user agent)
2. Пользовательские стили
3. Авторские стили (ваш CSS)
4. Inline стили

```html
<div style="color: red;">Inline стиль</div>
```

## 9.2 Специфичность селекторов

Специфичность определяет, какой стиль будет применен:

| Селектор | Специфичность | Пример |
|----------|--------------|--------|
| Элемент | 1 | `h1` |
| Класс | 10 | `.title` |
| ID | 100 | `#main` |
| Inline | 1000 | `style=""` |

### Примеры:

```css
/* Специфичность: 1 */
h1 { color: black; }

/* Специфичность: 10 */
.title { color: blue; }

/* Специфичность: 11 */
h1.title { color: red; }

/* Специфичность: 100 */
#main { color: green; }
```

⚠️ **Совет:** Избегайте ID в CSS, используйте классы!

## 9.3 Организация CSS кода

### Группировка стилей:

```css
/* =================
   Общие стили
   ================= */

/* Сброс */
*, *::before, *::after {
    box-sizing: border-box;
}

/* Основные */
body {
    margin: 0;
    font-family: Arial, sans-serif;
}

/* =================
   Шапка
   ================= */
.page-header {
    /* стили */
}

/* =================
   Навигация
   ================= */
.navigation {
    /* стили */
}
```

## 9.4 CSS переменные (Custom Properties)

```css
:root {
    --color-primary: #82b3d3;
    --color-text: #333333;
    --font-main: "PT Sans", Arial, sans-serif;
    --spacing-large: 96px;
}

.button-primary {
    background-color: var(--color-primary);
}

.advantages {
    padding: var(--spacing-large) 0;
}
```

### Преимущества переменных:
- ✅ Легко изменить цвета сайта
- ✅ Переиспользуемость
- ✅ Динамическое изменение через JavaScript

## 9.5 Единицы измерения

### Абсолютные:
- `px` - пиксели

### Относительные:
- `%` - проценты от родителя
- `em` - относительно размера шрифта элемента
- `rem` - относительно размера шрифта корневого элемента
- `vh, vw` - проценты от высоты/ширины viewport

```css
.container {
    width: 1200px;      /* Абсолютная */
    max-width: 100%;    /* Относительная */
    padding: 2rem;      /* Относительная */
    height: 50vh;       /* Относительная */
}
```

## 9.6 Box Model (Блочная модель)

```
+------------------+
|     margin       |
|  +------------+  |
|  |  border    |  |
|  | +--------+ |  |
|  | |padding | |  |
|  | |+------+| |  |
|  | ||content|| |  |
|  | |+------+| |  |
|  | +--------+ |  |
|  +------------+  |
+------------------+
```

```css
.box {
    width: 200px;
    padding: 20px;
    border: 2px solid black;
    margin: 10px;
    box-sizing: border-box; /* Важно! */
}
```

### box-sizing:
- `content-box` (по умолчанию) - width не включает padding и border
- `border-box` - width включает padding и border

## 9.7 Лучшие практики

### 1. Используйте краткую запись:

❌ Плохо:
```css
.element {
    margin-top: 10px;
    margin-right: 20px;
    margin-bottom: 10px;
    margin-left: 20px;
}
```

✅ Хорошо:
```css
.element {
    margin: 10px 20px;
}
```

### 2. Группируйте похожие свойства:

```css
.element {
    /* Позиционирование */
    position: relative;
    top: 0;
    left: 0;
    
    /* Блочная модель */
    display: block;
    width: 100%;
    padding: 20px;
    
    /* Типографика */
    font-size: 16px;
    color: #000;
    
    /* Визуал */
    background: #fff;
    border: 1px solid #ccc;
}
```

### 3. Избегайте !important:

❌ Плохо:
```css
.title {
    color: red !important;
}
```

✅ Хорошо: увеличьте специфичность
```css
.page-main .title {
    color: red;
}
```

## 9.8 CSS Reset vs Normalize

### Reset - сбрасывает все стили:
```css
* {
    margin: 0;
    padding: 0;
}
```

### Normalize - нормализует стили между браузерами:
```css
/* normalize.css */
```

💡 **Рекомендация:** Используйте Normalize.css + свой мини-reset

## Практическое задание

1. ✅ Организуйте CSS по секциям с комментариями
2. ✅ Создайте CSS переменные для цветов
3. ✅ Проверьте специфичность селекторов
4. ✅ Используйте box-sizing: border-box
5. ✅ Группируйте похожие свойства

## Контрольные вопросы

1. Что такое специфичность селекторов?
2. Какая разница между em и rem?
3. Зачем нужен box-sizing: border-box?
4. Когда использовать CSS переменные?

## Полезные ссылки

- [CSS Specificity Calculator](https://specificity.keegan.st/)
- [CSS Variables Guide](https://developer.mozilla.org/ru/docs/Web/CSS/Using_CSS_custom_properties)
- [Box Model на MDN](https://developer.mozilla.org/ru/docs/Learn/CSS/Building_blocks/The_box_model)

---

[⬅️ Предыдущий урок](lesson-08-footer.md) | [Следующий урок: Адаптивный дизайн ➡️](lesson-10-responsive-design.md)
