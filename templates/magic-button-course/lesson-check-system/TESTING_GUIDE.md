# 🧪 Инструкция по тестированию системы проверки с рекламой

## 📋 Обзор системы

Система включает:

- **Проверка кода**: Анализ JavaScript кода ученика
- **Система подсказок**: Кнопки с пронумерованными подсказками
- **Реклама**: Обязательный просмотр перед показом подсказки
- **Тестовый режим**: Возможность тестирования всех функций

## 🚀 Как запустить тест

### Способ 1: Открыть в браузере

```bash
# Откройте файл в браузере:
file:///d:/02_HTML_CURS/templates/magic-button-course/lesson-check-system/index.html
```

### Способ 2: Локальный сервер

```bash
# В корне проекта:
cd d:\02_HTML_CURS\templates\magic-button-course\lesson-check-system
python -m http.server 8000
# Откройте: http://localhost:8000
```

## 🔧 Тестовые функции

### Активация тестового режима

Тестовый режим активен по умолчанию (`TEST_MODE = true`). В консоли браузера доступны:

```javascript
// Тестировать рекламу
testAd();

// Показать все подсказки
testHints();

// Симулировать ошибки
testErrors();

// Изменить время рекламы (только для демо)
setAdDuration(3); // 3 секунды вместо 5
```

## 📝 Сценарии тестирования

### 1. Тестирование с пустым кодом

1. Оставьте поле кода пустым
2. Нажмите "🔍 Проверить код"
3. **Ожидается**: Сообщение об ошибке "Пожалуйста, введите код"

### 2. Тестирование с неполным кодом

Введите в поле кода:

```javascript
let count = 0;
```

1. Нажмите "🔍 Проверить код"
2. **Ожидается**: Появятся кнопки "💡 Подсказка 1", "💡 Подсказка 2", etc.

### 3. Тестирование рекламы

1. Нажмите любую кнопку подсказки
2. **Ожидается**:
   - Появится модальное окно с рекламой
   - Таймер 5 секунд
   - Кнопка "Пропустить рекламу" заблокирована
   - После 5 сек кнопка активируется

### 4. Тестирование подсказок

1. Дождитесь окончания рекламы
2. Нажмите "Продолжить"
3. **Ожидается**: Появится подсказка с детальным объяснением

### 5. Тестирование правильного кода

Введите правильный код:

```javascript
let clickCount = 0;
const magicButton = document.querySelector(".magic-button");

magicButton.addEventListener("click", function () {
  clickCount++;
  console.log("Кликов:", clickCount);
});
```

1. Нажмите "🔍 Проверить код"
2. **Ожидается**: Сообщение "🎉 Отлично! Код написан правильно!"

## 🎯 Тестирование рекламы в продакшене

### Варианты интеграции реальной рекламы:

#### 1. Google AdSense

```javascript
// Замените демо-рекламу на:
function showRealAd(callback) {
  // Загружаем рекламный блок
  (adsbygoogle = window.adsbygoogle || []).push({
    google_ad_client: "ca-pub-XXXXXXXXXXXXXXXX",
    enable_page_level_ads: true,
  });

  // Ждем завершения показа
  setTimeout(callback, 5000); // или используйте события AdSense
}
```

#### 2. Яндекс.Директ

```javascript
function showYandexAd(callback) {
  // Интеграция с Яндекс.Директ API
  Ya.Context.AdvManager.render({
    blockId: "R-A-XXXXXX-X",
    renderTo: "ad-container",
    onRender: callback,
  });
}
```

#### 3. Собственная рекламная сеть

```javascript
function showCustomAd(callback) {
  fetch("/api/ads/get-random")
    .then((response) => response.json())
    .then((adData) => {
      // Показываем рекламу
      displayCustomAd(adData);
      // Отслеживаем показ
      setTimeout(() => {
        trackAdView(adData.id);
        callback();
      }, adData.duration * 1000);
    });
}
```

## 🛠 Настройки для тестирования

### В script.js измените:

```javascript
// Тестовый режим
const TEST_MODE = true; // false для продакшена

// Время рекламы
const AD_DURATION = 5; // секунды (для теста можно 2-3)

// Принудительная реклама
const FORCE_AD = true; // true = всегда показывать

// Логирование
const DEBUG_MODE = true; // подробные логи в консоли
```

## 📊 Мониторинг и аналитика

### Добавьте отслеживание:

```javascript
// Метрики для тестирования
const analytics = {
  hintsRequested: 0,
  adsShown: 0,
  adsCompleted: 0,
  hintsViewed: 0,
};

function trackEvent(event, data) {
  analytics[event]++;
  console.log(`📊 ${event}:`, data, "Всего:", analytics[event]);

  // Отправка в аналитику
  if (!TEST_MODE) {
    gtag("event", event, data);
  }
}
```

## ⚠️ Важные замечания

1. **Блокировщики рекламы**: Тестируйте с отключенными и включенными блокировщиками
2. **Мобильные устройства**: Проверьте адаптивность рекламных блоков
3. **Производительность**: Следите за временем загрузки рекламы
4. **Пользовательский опыт**: Не делайте рекламу слишком навязчивой

## 🎮 Дополнительные возможности для тестирования

### Симуляция разных типов ошибок:

```javascript
// В консоли браузера:
testErrors(); // Покажет все типы ошибок

// Индивидуальные тесты:
showErrors(["missing_variable"]); // Только ошибка переменной
showErrors(["missing_element", "missing_event_listener"]); // Несколько ошибок
```

### Тестирование performance:

```javascript
// Замер времени показа рекламы
performance.mark("ad-start");
showAd(() => {
  performance.mark("ad-end");
  console.log(
    "Время показа рекламы:",
    performance.measure("ad-duration", "ad-start", "ad-end")
  );
});
```

## 🔄 Автоматические тесты

Можно добавить автотесты с помощью Selenium или Playwright:

```javascript
// Пример теста (псевдокод)
test("Реклама показывается перед подсказкой", async () => {
  await page.click(".check-button");
  await page.click(".hint-button");

  const adModal = await page.waitForSelector(".ad-modal");
  expect(adModal).toBeVisible();

  await page.waitForTimeout(5000);
  await page.click("#skipAdBtn");

  const hintModal = await page.waitForSelector(".hint-modal");
  expect(hintModal).toBeVisible();
});
```

---

**Готово для тестирования!** 🎉

Откройте [index.html](index.html) в браузере и начните тестирование системы.
