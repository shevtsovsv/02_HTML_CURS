# Практикум: Создание игры "Волшебная кнопка"

Добро пожаловать в пошаговый практикум по созданию вашей первой интерактивной игры!
Следуйте инструкциям поэтапно и создайте волшебную кнопку с красивыми эффектами.

# Урок 1: Создаем базовую HTML структуру

# Урок 2: Создаем стили CSS

## 2.1 Настройте правило для body:

### 2.1.1 Подключите шрифты:

Подключите шрифты: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif

**Проверка:**

- Наличие CSS правила для `body`
- Свойство `font-family` со значением `"Segoe UI", Tahoma, Geneva, Verdana, sans-serif`

**Подсказка:**

```css
font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
```

2.1.2 Настрой линейный градиент с параметрами 135deg, #667eea 0%, #764ba2 100%
Подсказка:
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
2.1.3 Сделай нулевые внешние и внутренние отступы
Подсказка:
margin: 0;
padding: 0;
2.1.4 Установи высоту в 100% видимой части экрана
Подсказка
min-height: 100vh;
2.1.5 Спомощью Flex расположи содержимое по центру экрана
Подсказка
display: flex;
align-items: center;
justify-content: center;
2.1.6 Установи цвет шрифта white
Подсказка: color: white;

Итоговая подсказка:
body {
font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
margin: 0;
padding: 0;
min-height: 100vh;
display: flex;
align-items: center;
justify-content: center;
color: white;
}

# Урок 1: Создаем базовую HTML структуру

## 1.1 Добавьте базовую структуру HTML:

Начните с <!DOCTYPE html> и добавьте теги <html>, <head> и <body>

**Проверка:**

- Наличие тега `<!DOCTYPE html>`
- Наличие тега `<html>` с атрибутом `lang="ru"`
- Наличие тегов `<head>` и `<body>`

**Подсказка:**

```html
<!DOCTYPE html>
<html lang="ru">
  <head> </head>
  <body></body>
</html>
```

## 1.2 Заполните секцию head:

Добавьте title и meta теги для правильного отображения

**Проверка:**

- Наличие тега `<meta charset="UTF-8">`
- Наличие тега `<meta name="viewport" content="width=device-width, initial-scale=1.0">`
- Наличие тега `<title>Волшебная кнопка</title>`

**Подсказка:**

```html
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Волшебная кнопка</title>
</head>
```

## 1.3 Создайте контент в body:

В body добавьте заголовок первого уровня, описание и кнопку

**Проверка:**

- Наличие тега `<div>` внутри `<body>`
- Наличие тега `<h1>` с текстом "Волшебная кнопка"
- Наличие тега `<p>` с текстом "Нажми на кнопку и увидишь магию!"
- Наличие тега `<button>` с текстом "✨ Магия! ✨"

**Подсказка:**

```html
<body>
  <div>
    <h1>Волшебная кнопка</h1>
    <p>Нажми на кнопку и увидишь магию!</p>
    <button>✨ Магия! ✨</button>
  </div>
</body>
```

# Урок 2: Создаем стили CSS

## 2.1 Настройте правило для body:

### 2.1.1 Подключите шрифты:

Подключите шрифты: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif

**Проверка:**

- Наличие CSS правила для `body`
- Свойство `font-family` со значением `"Segoe UI", Tahoma, Geneva, Verdana, sans-serif`

**Подсказка:**

```css
font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
```

### 2.1.2 Настройте градиентный фон:

Настройте линейный градиент с параметрами 135deg, #667eea 0%, #764ba2 100%

**Проверка:**

- Свойство `background` со значением `linear-gradient(135deg, #667eea 0%, #764ba2 100%)`

**Подсказка:**

```css
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
```

### 2.1.3 Уберите отступы:

Сделайте нулевые внешние и внутренние отступы

**Проверка:**

- Свойство `margin: 0`
- Свойство `padding: 0`

**Подсказка:**

```css
margin: 0;
padding: 0;
```

### 2.1.4 Установите высоту:

Установите высоту в 100% видимой части экрана

**Проверка:**

- Свойство `min-height: 100vh`

**Подсказка:**

```css
min-height: 100vh;
```

### 2.1.5 Центрируйте содержимое:

С помощью Flexbox расположите содержимое по центру экрана

**Проверка:**

- Свойство `display: flex`
- Свойство `align-items: center`
- Свойство `justify-content: center`

**Подсказка:**

```css
display: flex;
align-items: center;
justify-content: center;
```

### 2.1.6 Установите цвет текста:

Установите цвет шрифта белый

**Проверка:**

- Свойство `color: white`

**Подсказка:**

```css
color: white;
```

**Итоговое CSS правило для body:**

```css
body {
  font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  margin: 0;
  padding: 0;
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  color: white;
}
```

## 2.2 Настройте стили для контейнера:

### 2.2.1 Добавьте класс к div:

Тегу div присвойте класс game-container

**Проверка:**

- Атрибут `class="game-container"` у тега `<div>`

**Подсказка:**

```html
<div class="game-container"></div>
```

### 2.2.2 Центрируйте текст:

Расположите текст по центру в game-container

**Проверка:**

- Наличие CSS правила для `.game-container`
- Свойство `text-align: center`

**Подсказка:**

```css
.game-container {
  text-align: center;
}
```

## 2.3 Стилизуйте заголовок:

Добавьте стили для заголовка h1

**Проверка:**

- Наличие CSS правила для `h1`
- Свойство `font-size: 2.5rem`
- Свойство `margin-bottom: 10px`
- Свойство `text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3)`

**Подсказка:**

```css
h1 {
  font-size: 2.5rem;
  margin-bottom: 10px;
  text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
}
```

## 2.4 Добавьте стили для параграфа:

Создайте класс subtitle для описания

**Проверка:**

- Атрибут `class="subtitle"` у тега `<p>`
- Наличие CSS правила для `.subtitle`
- Свойство `font-size: 1.2rem`
- Свойство `margin-bottom: 30px`
- Свойство `opacity: 0.9`

**Подсказка:**

```html
<p class="subtitle">Нажми на кнопку и увидишь магию!</p>
```

```css
.subtitle {
  font-size: 1.2rem;
  margin-bottom: 30px;
  opacity: 0.9;
}
```

# Урок 3: Стилизация кнопки

## 3.1 Создайте класс для кнопки:

Добавьте класс magic-button к кнопке

**Проверка:**

- Атрибут `class="magic-button"` у тега `<button>`

**Подсказка:**

```html
<button class="magic-button">✨ Магия! ✨</button>
```

## 3.2 Стилизуйте кнопку:

Добавьте красивые стили для волшебной кнопки

**Проверка:**

- Наличие CSS правила для `.magic-button`
- Свойство `background: linear-gradient(135deg, #ff6b6b, #ffa500)`
- Свойство `color: white`
- Свойство `border: none`
- Свойство `padding: 20px 40px`
- Свойство `font-size: 1.5rem`
- Свойство `border-radius: 50px`
- Свойство `cursor: pointer`

**Подсказка:**

```css
.magic-button {
  background: linear-gradient(135deg, #ff6b6b, #ffa500);
  color: white;
  border: none;
  padding: 20px 40px;
  font-size: 1.5rem;
  font-weight: bold;
  border-radius: 50px;
  cursor: pointer;
  margin: 20px;
  box-shadow: 0 10px 25px rgba(255, 107, 107, 0.4);
  transition: all 0.3s ease;
  text-transform: uppercase;
  letter-spacing: 1px;
}
```

## 3.3 Добавьте эффект при наведении:

Создайте анимацию при наведении мыши на кнопку

**Проверка:**

- Наличие CSS правила для `.magic-button:hover`
- Свойство `transform: scale(1.1) rotate(2deg)`
- Свойство `box-shadow: 0 15px 35px rgba(255, 107, 107, 0.6)`

**Подсказка:**

```css
.magic-button:hover {
  transform: scale(1.1) rotate(2deg);
  box-shadow: 0 15px 35px rgba(255, 107, 107, 0.6);
  background: linear-gradient(135deg, #ff5252, #ff8f00);
}
```

## 3.4 Добавьте эффект при нажатии:

Создайте анимацию при нажатии на кнопку

**Проверка:**

- Наличие CSS правила для `.magic-button:active`
- Свойство `transform: scale(0.95) rotate(-1deg)`

**Подсказка:**

```css
.magic-button:active {
  transform: scale(0.95) rotate(-1deg);
}
```

# Урок 4: Добавляем JavaScript логику

## 4.1 Создайте переменную для счетчика:

Добавьте JavaScript переменную для подсчета кликов

**Проверка:**

- Наличие тега `<script>` в конце `<body>`
- Объявление переменной `let clickCount = 0;` внутри скрипта

**Подсказка:**

```html
<script>
  let clickCount = 0;
</script>
```

## 4.2 Получите ссылку на кнопку:

Создайте переменную для работы с кнопкой

**Проверка:**

- Наличие переменной `const magicButton = document.querySelector('.magic-button');`

**Подсказка:**

```javascript
const magicButton = document.querySelector(".magic-button");
```

## 4.3 Добавьте обработчик события:

Создайте функцию, которая будет выполняться при клике на кнопку

**Проверка:**

- Наличие `magicButton.addEventListener('click', function() { ... });`
- Увеличение счетчика `clickCount++`

**Подсказка:**

```javascript
magicButton.addEventListener("click", function () {
  clickCount++;
  console.log("Кликов:", clickCount);
});
```

## 4.4 Добавьте случайные фразы:

Создайте массив с волшебными фразами и выводите их случайным образом

**Проверка:**

- Наличие массива `const magicPhrases`
- Использование `Math.random()` и `Math.floor()`
- Вывод фразы через `alert()`

**Подсказка:**

```javascript
const magicPhrases = [
  "✨ Магия работает! ✨",
  "🌟 Невероятно! 🌟",
  "🎭 Удивительно! 🎭",
  "🔮 Волшебство! 🔮",
  "🎪 Фантастика! 🎪",
];

magicButton.addEventListener("click", function () {
  clickCount++;
  const randomPhrase =
    magicPhrases[Math.floor(Math.random() * magicPhrases.length)];
  alert(randomPhrase);
});
```

# Урок 5: Добавляем интерфейс статистики

## 5.1 Добавьте контейнер для статистики:

Создайте div для отображения счетчика

**Проверка:**

- Наличие тега `<div class="stats">` после кнопки
- Наличие элемента с `id="click-counter"`

**Подсказка:**

```html
<div class="stats">
  <h3>Статистика магии:</h3>
  <p>Кликов: <span id="click-counter">0</span></p>
</div>
```

## 5.2 Стилизуйте панель статистики:

Добавьте CSS стили для панели

**Проверка:**

- Наличие CSS правила для `.stats`
- Свойство `background: rgba(255, 255, 255, 0.2)`
- Свойство `padding: 20px`
- Свойство `border-radius: 15px`
- Свойство `margin-top: 30px`

**Подсказка:**

```css
.stats {
  background: rgba(255, 255, 255, 0.2);
  padding: 20px;
  border-radius: 15px;
  margin-top: 30px;
  border: 1px solid rgba(255, 255, 255, 0.3);
}
```

## 5.3 Обновляйте счетчик в интерфейсе:

Измените JavaScript, чтобы обновлялся счетчик на экране

**Проверка:**

- Получение элемента через `document.getElementById('click-counter')`
- Обновление содержимого элемента через `.textContent`

**Подсказка:**

```javascript
const clickCounterElement = document.getElementById("click-counter");

magicButton.addEventListener("click", function () {
  clickCount++;
  clickCounterElement.textContent = clickCount;

  const randomPhrase =
    magicPhrases[Math.floor(Math.random() * magicPhrases.length)];
  alert(randomPhrase);
});
```

# Урок 6: Финальные улучшения

## 6.1 Добавьте красивый контейнер:

Оберните game-container в стеклянный дизайн

**Проверка:**

- Обновление CSS правила для `.game-container`
- Свойство `background: rgba(255, 255, 255, 0.1)`
- Свойство `backdrop-filter: blur(10px)`
- Свойство `border-radius: 20px`
- Свойство `padding: 40px`

**Подсказка:**

```css
.game-container {
  text-align: center;
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(10px);
  border-radius: 20px;
  padding: 40px;
  border: 1px solid rgba(255, 255, 255, 0.2);
  box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
  max-width: 500px;
}
```

## 6.2 Добавьте звуковые эффекты:

Создайте функцию для воспроизведения звуков (по желанию)

**Проверка:**

- Функция создания звука через Web Audio API или Audio объект
- Вызов функции в обработчике клика

**Подсказка:**

```javascript
function playClickSound() {
  // Создаем звук через Web Audio API
  const audioContext = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioContext.createOscillator();
  const gainNode = audioContext.createGain();

  oscillator.connect(gainNode);
  gainNode.connect(audioContext.destination);

  oscillator.frequency.value = 800;
  oscillator.type = "sine";

  gainNode.gain.setValueAtTime(0.3, audioContext.currentTime);
  gainNode.gain.exponentialRampToValueAtTime(
    0.01,
    audioContext.currentTime + 0.3
  );

  oscillator.start(audioContext.currentTime);
  oscillator.stop(audioContext.currentTime + 0.3);
}
```

## 6.3 Добавьте достижения:

Создайте систему достижений за определенное количество кликов

**Проверка:**

- Проверка количества кликов в обработчике
- Отображение сообщений о достижениях
- Специальные эффекты при достижении целей

**Подсказка:**

```javascript
function checkAchievements() {
  if (clickCount === 10) {
    alert("🏆 Достижение: Первые 10 кликов!");
  } else if (clickCount === 50) {
    alert("🏆 Достижение: Полсотни кликов!");
  } else if (clickCount === 100) {
    alert("🏆 Достижение: Сотня кликов! Вы мастер магии!");
  }
}
```

## 6.4 Финальная проверка:

Убедитесь, что все работает правильно

**Проверка:**

- Страница загружается корректно
- Кнопка реагирует на клики
- Счетчик увеличивается при каждом клике
- Отображаются случайные фразы
- Работают анимации и эффекты
- Стили применяются корректно

**Поздравляем! Вы создали свою первую интерактивную игру! 🎉**
